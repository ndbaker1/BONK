import { PlayerData } from '../shared-types'
import { Area } from './game-renderer'

export const Players = {
  render(canvasContext: CanvasRenderingContext2D, dimensions: Area, playerData: PlayerData[]): void {
    //
  }
}